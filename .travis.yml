language: generic

sudo: required
services: 
  - docker

before_install:
  - docker build -t simple-docker-app -f Dockerfile.dev .

script: 
  - docker run simple-docker-app npm run test -- --coverage
